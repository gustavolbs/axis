name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build and test
        run: npm run check

  windows-build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript on Windows
        run: npm run build

      - name: Run baseline remote-agent tests on Windows
        run: >-
          npx tsx --test
          test/remote-execution.test.ts
          test/worker-workspace.test.ts
          test/worker-scheduler.test.ts
          test/local-engineer.test.ts

      - name: Run repo-intelligence fingerprint scenario on Windows
        run: >-
          npx tsx --test
          --test-name-pattern="fingerprints dirty"
          test/repo-intelligence.test.ts

      - name: Run repo-intelligence isolation scenario on Windows
        if: always()
        run: >-
          npx tsx --test
          --test-name-pattern="isolates repositories"
          test/repo-intelligence.test.ts

      - name: Run Qwen3.8 reasoning tests on Windows
        if: always()
        run: npx tsx --test test/qwen38-thinking.test.ts

      - name: Check Node installer syntax
        if: always()
        shell: pwsh
        run: |
          node --check scripts/install-claude-windows-remote.mjs
          node --check scripts/install-claude-remote-worker.mjs

      - name: Check PowerShell installer syntax
        if: always()
        shell: pwsh
        run: |
          $scripts = @(
            '.\scripts\setup-windows-host.ps1',
            '.\scripts\install-windows-worker-task.ps1'
          )
          foreach ($script in $scripts) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              (Resolve-Path $script),
              [ref]$tokens,
              [ref]$errors
            ) | Out-Null
            if ($errors.Count -gt 0) {
              $errors | ForEach-Object { Write-Error "${script}: $($_.Message)" }
              exit 1
            }
          }
